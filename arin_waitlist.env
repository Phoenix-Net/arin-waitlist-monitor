# ARIN IPv4 Waiting List Monitor configuration
#
# Copy this file to .env (or keep the name arin_waitlist.env).
# The script loads whichever it finds first next to arin_waitlist.py.

# This MUST exactly match the "Date and Time Added to Waiting List"
# shown on https://www.arin.net/resources/guide/ipv4/waiting_list/
ARIN_TARGET_DATE="Tue, 03 Feb 2026, 12:17:25 EST"

# --- Fluxer incoming webhook ---
# Create a webhook in the destination channel (channel settings → Integrations
# → Webhooks) and paste the full URL here. Self-hosted instances work the same
# way — use the URL your instance gives you.
#
# Official:  https://api.fluxer.app/v1/webhooks/{id}/{token}
# Self-host: https://fluxer.example.com/v1/webhooks/{id}/{token}
FLUXER_WEBHOOK_URL="CHANGE_ME"

# Optional display name override for the webhook posts
# FLUXER_WEBHOOK_USERNAME="ARIN Waitlist"

# --- Email (optional; leave blank to skip) ---
# Port 465 = SMTPS (implicit TLS)
# Port 587 = SMTP with STARTTLS
SMTP_HOST=""
SMTP_PORT="465"
SMTP_USER=""
SMTP_PASS=""

# Email settings
MAIL_FROM=""

# Multiple recipients are supported:
# comma, semicolon, or space separated
MAIL_TO=""

MAIL_SUBJECT_PREFIX="[ARIN Waitlist]"
